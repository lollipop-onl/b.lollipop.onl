import React, { FC } from 'react';
import { Layout } from '~/components/templates/Layout';

type Props = {};

export const config = { amp: true };

export const IndexPage: FC<Props> = () => <Layout title="b.lollipop.onl">hoo</Layout>;

export default IndexPage;
